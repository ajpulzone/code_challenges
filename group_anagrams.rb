#Write a method to sort an array of strings so that all the anagrams are mext to each other.

def group_anagrams(array)
  # anagram_array = []
  # not_anagram_array = []

  # array.each do |string|
  #   if string == string.reverse
  # end
end